/**
 * Tailwind 4 uses the @tailwindcss/postcss plugin.
 * No tailwind.config.js needed — Tailwind 4 is config-less via CSS @import.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
