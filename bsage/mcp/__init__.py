"""bsage.mcp — Model Context Protocol server (stdio + SSE) and helpers.

This package hosts the real MCP protocol implementation plus the
``plugin_bridge`` adapter that exposes input plugins as MCP tools.

Phase 4a ships only ``plugin_bridge``. The stdio/SSE transport servers
are added in Phase 4b.
"""
